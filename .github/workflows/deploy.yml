name: Deploy to OMV

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Deploy via SCP (SSH)
      uses: appleboy/scp-action@v0.1.7
      with:
        host: 192.168.100.5
        username: root
        password: 4cRcg12gfo
        port: 22
        source: "."
        target: "/srv/dev-disk-by-uuid-310ff77b-00b4-4054-80af-1595c90f666a/Data/webapi"
        strip_components: 0